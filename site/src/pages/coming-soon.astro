---
import Frame from "../layouts/frame.astro";
import Nav from "../components/Nav.astro";
---

<Frame
	classes='flex-col'
	title="Bientôt disponible - ECollAct"
	description="Nous préparons quelque chose d'incroyable pour améliorer la qualité de l'air. Revenez bientôt !"
	keywords="ECollAct, bientôt disponible, qualité air, innovation">
	<Nav />
	<section class="min-h-screen flex items-center justify-center px-8 py-24 bg-gradient-to-br from-white via-stone/20 to-olive/5 relative overflow-hidden" aria-label="Page bientôt disponible">
		<!-- Background Pattern -->
		<div class="absolute inset-0 opacity-10">
			<div class="absolute top-20 left-20 w-64 h-64 bg-olive rounded-full blur-3xl animate-float"></div>
			<div class="absolute bottom-20 right-20 w-80 h-80 bg-blue rounded-full blur-3xl animate-float-delayed"></div>
			<div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-96 h-96 bg-orange rounded-full blur-3xl animate-pulse-slow"></div>
		</div>

		<div class="max-w-4xl mx-auto text-center relative z-10">
			<!-- Icon -->
			<div class="mb-8 animate-bounce-slow">
				<svg class="w-24 h-24 mx-auto text-olive" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
					<path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
				</svg>
			</div>

			<!-- Main Title -->
			<h1 class="text-5xl md:text-7xl font-bold text-night mb-6 animate-fade-in">
				Nous arrivons <span class="text-olive">bientôt</span>
			</h1>

			<!-- Description -->
			<p class="text-xl md:text-2xl text-night/70 mb-12 leading-relaxed max-w-2xl mx-auto animate-fade-in-delay">
				Nous travaillons activement à vous offrir la meilleure expérience pour surveiller et améliorer
				la qualité de l'air. Revenez très prochainement !
			</p>

			<!-- Features Grid -->
			<div class="grid md:grid-cols-3 gap-6 mb-12 animate-fade-in-delay-2">
				<div class="bg-white/60 backdrop-blur-sm p-6 rounded-2xl border border-olive/20 shadow-lg hover:shadow-xl transition-all duration-300 hover:-translate-y-1">
					<div class="text-3xl mb-3">🌱</div>
					<h3 class="font-bold text-night mb-2">Action collective</h3>
					<p class="text-sm text-night/60">Ensemble pour un air plus pur</p>
				</div>
				<div class="bg-white/60 backdrop-blur-sm p-6 rounded-2xl border border-blue/20 shadow-lg hover:shadow-xl transition-all duration-300 hover:-translate-y-1">
					<div class="text-3xl mb-3">📊</div>
					<h3 class="font-bold text-night mb-2">Données en temps réel</h3>
					<p class="text-sm text-night/60">Capteurs connectés performants</p>
				</div>
				<div class="bg-white/60 backdrop-blur-sm p-6 rounded-2xl border border-orange/20 shadow-lg hover:shadow-xl transition-all duration-300 hover:-translate-y-1">
					<div class="text-3xl mb-3">💡</div>
					<h3 class="font-bold text-night mb-2">Analyse intelligente</h3>
					<p class="text-sm text-night/60">Insights actionnables</p>
				</div>
			</div>

			<!-- Call to Action -->
			<div class="animate-fade-in-delay-3">
				<a
					href="/"
					class="inline-block px-10 py-4 bg-olive text-white rounded-xl hover:bg-olive/90 transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl"
					aria-label="Retour à l'accueil">
					Retour à l'accueil
				</a>
			</div>

			<!-- Progress Indicator -->
			<div class="mt-16 animate-fade-in-delay-3">
				<p class="text-sm text-night/50 mb-4">Progression du développement</p>
				<div class="max-w-md mx-auto bg-night/10 rounded-full h-3 overflow-hidden">
					<div class="bg-gradient-to-r from-olive to-blue h-full rounded-full animate-progress" style="width: 75%"></div>
				</div>
				<p class="text-sm text-night/50 mt-2">75% complété</p>
			</div>
		</div>
	</section>
</Frame>

<style>
	@keyframes fadeIn {
		from { opacity: 0; transform: translateY(20px); }
		to { opacity: 1; transform: translateY(0); }
	}

	@keyframes float {
		0%, 100% { transform: translate(0, 0); }
		50% { transform: translate(30px, -30px); }
	}

	@keyframes progress {
		0% { width: 0%; }
		100% { width: 75%; }
	}

	.animate-fade-in {
		animation: fadeIn 0.8s ease-out;
	}

	.animate-fade-in-delay {
		animation: fadeIn 0.8s ease-out 0.2s both;
	}

	.animate-fade-in-delay-2 {
		animation: fadeIn 0.8s ease-out 0.4s both;
	}

	.animate-fade-in-delay-3 {
		animation: fadeIn 0.8s ease-out 0.6s both;
	}

	.animate-bounce-slow {
		animation: bounce 3s ease-in-out infinite;
	}

	@keyframes bounce {
		0%, 100% { transform: translateY(0); }
		50% { transform: translateY(-10px); }
	}

	.animate-float {
		animation: float 8s ease-in-out infinite;
	}

	.animate-float-delayed {
		animation: float 10s ease-in-out infinite 2s;
	}

	.animate-pulse-slow {
		animation: pulse 4s ease-in-out infinite;
	}

	@keyframes pulse {
		0%, 100% { opacity: 0.1; }
		50% { opacity: 0.15; }
	}

	.animate-progress {
		animation: progress 2s ease-out 0.8s both;
	}
</style>
